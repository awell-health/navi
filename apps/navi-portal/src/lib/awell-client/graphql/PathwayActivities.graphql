# import 'Activity'
query PathwayActivities($careflow_id: String!) {
  pathwayActivities(
    careflow_id: $careflow_id
    sorting: { field: "date", direction: "ASC" }
  ) {
    success
    activities {
      ...Activity
    }
    totalCount
  }
}
